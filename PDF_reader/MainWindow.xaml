<Window x:Class="PDF_reader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PDF Reader" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <!-- Define Row structure for layout -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Menu Bar -->
            <RowDefinition Height="Auto" />
            <!-- Toolbar -->
            <RowDefinition Height="*" />
            <!-- PDF Viewer -->
            <RowDefinition Height="Auto" />
            <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Menu Grid.Row="0">
            <MenuItem Header="_File">
                <MenuItem Header="Open" Click="OpenFile_Click"/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Zoom In" Click="ZoomIn_Click"/>
                <MenuItem Header="Zoom Out" Click="ZoomOut_Click"/>
            </MenuItem>
            <MenuItem Header="Explore Books" Click="Books_clicked"/>
            <MenuItem Header="_Help">
                <MenuItem Header="About" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <!-- Toolbar (Search) -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <TextBox x:Name="SearchBox" Width="300" Text="Search here..." 
         GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus"/>
            <Button Content="Search" Width="100" Margin="5,0,0,0" Click="Search_Click"/>
            <Button Content="Previous" Width="100" Click="PreviousPage_Click"/>
            <Button Content="Next" Width="100" Click="NextPage_Click"/>
        </StackPanel>

        <!-- PDF Viewer -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" Margin="10">
            <Image x:Name="PdfImage" Stretch="Uniform" />
        </Border>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="PageNumberText" Text="Page: 0 / 0" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="ZoomLevelText" Text="Zoom: 100%" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
